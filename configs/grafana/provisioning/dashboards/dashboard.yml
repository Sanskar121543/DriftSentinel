apiVersion: 1

providers:
  - name: DriftSentinel
    orgId: 1
    folder: DriftSentinel
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
